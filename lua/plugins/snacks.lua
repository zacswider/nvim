return {
  'folke/snacks.nvim',
  priority = 1000,
  lazy = false,
  opts = {
    -- Floating input box — used by opencode.nvim's ask()
    input = {},
    -- Picker — used by opencode.nvim's select()
    picker = {},
  },
}
